#if ($body)
<Frame #if($bad)bad="true"#end>
   <SessionId>$sessionId</SessionId>
   <SessionHost>$sessionHost</SessionHost>
#if ($frameType)
   <FrameType>$frameType</FrameType>
#end
#if ($ert)
   <ERT>$ert</ERT>
#end
#if ($rct)
   <RCT>$rct</RCT>
#end
#if ($relayScid)
   <RelaySCID>$relayScid</RelaySCID>
#end
#if ($vcid)
   <VCID>$vcid</VCID>
#end
#if ($vcfc)
   <VCFC>$vcfc</VCFC>
#end
#if ($dssId >= 0)
   <DssId>$dssId</DssId>
#end
#if ($bitRate)
   <BitRate>$bitRate</BitRate>
#end
   <IsBad>$bad</IsBad>
#if ($badReason)
   <BadReason>$badReason>/BadReason>
#end   
#if ($length)
   <Length>$length</Length>
#end
#if ($fileByteOffset)
   <FileByteOffset>$fileByteOffset</FileByteOffset>
#end
</Frame>
#end
